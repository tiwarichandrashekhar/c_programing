#include <stdio.h>

int main() {
    int num,n;
   printf("enter the number");
   scanf("%d",&num);
   while(num>0)
   {
      n= num%10;
      printf("%d",n);
      num=num/10;
   }

    return 0;
}
